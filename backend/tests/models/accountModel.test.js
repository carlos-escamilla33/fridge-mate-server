const accountModel = require("../../src/models/accountModel");
const pool = require("../../src/models/accountModel");

describe("account model", () => {
    test("should create an account", async () => {
        const accountData = {
            accountName: "Test Family",
            accountCode: "TEST123"
        };
    });
});
